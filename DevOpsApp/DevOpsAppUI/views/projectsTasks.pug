extends layout

block content

  h1 Tareas del Proyecto #{projectId}

  a(href=`/projectsTasksInsert/${projectId}`) Agregar Nueva Tarea

  if tasks.length
    table
      thead
        tr
          th ID
          th Título
          th Descripción
          th Estado
          th Acciones
      tbody
        each task in tasks
          tr
            td= task.taskId
            td= task.title
            td= task.description
            td
              button(
                class="task-status",
                data-task-id=task.taskId,
                onclick="alterStatus(this)"
              )= task.isDone ? 'Done' : 'ToDo'

            td
              a(href=`/projectsTasksUpdate/${task.taskId}`) Editar
                
  else
    p No hay tareas registradas aún.
